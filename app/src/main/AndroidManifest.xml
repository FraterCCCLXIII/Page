<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Page requests NO runtime or network permissions.
      It is fully offline and privacy-first. The only system capability it
      binds to is the optional notification listener (declared on the service
      below), which the user must explicitly grant in system settings.

      Package visibility (Android 11+, API 30): instead of the heavy-handed
      QUERY_ALL_PACKAGES permission, we declare a narrow <queries> block so we
      can enumerate launchable apps and resolve the handful of intents we use.
    -->
    <queries>
        <!-- Enumerate launchable apps for the app list and search. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Resolve common actions used by the home-screen shortcuts. -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT" />
            <data android:mimeType="vnd.android.cursor.dir/event" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Page"
        tools:targetApi="31">

        <!--
          PageLauncherActivity is the home screen.
          MAIN + HOME + DEFAULT make it selectable as the default launcher.
          LAUNCHER places its single icon in app drawers so it can be opened
          like a normal app during setup (before it is the default home).
        -->
        <activity
            android:name=".PageLauncherActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|smallestScreenSize|density|uiMode"
            android:theme="@style/Theme.Page"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          MainActivity is a thin programmatic entry point (not a launcher icon).
          It simply forwards to PageLauncherActivity so the launcher UI can be
          opened from code, deep links, or development tooling without creating
          a duplicate home-screen icon.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Page" />

        <!--
          Notification listener stub. The user must explicitly enable this in
          system settings; Android guards it with BIND_NOTIFICATION_LISTENER_SERVICE.
          The service does nothing intrusive yet (see PageNotificationListener).
        -->
        <service
            android:name=".notifications.PageNotificationListener"
            android:exported="false"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
